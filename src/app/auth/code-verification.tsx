import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const CodeVerification = (): React.JSX.Element => {
  return (
    <View>
      <Text>CodeVerification</Text>
    </View>
  );
};

export default CodeVerification;

const styles = StyleSheet.create({});
